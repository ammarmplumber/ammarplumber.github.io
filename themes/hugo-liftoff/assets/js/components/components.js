var switcher = require('./switchTheme').switcher;
var clipboard = require('./clipboard').clipboard;
var toggleToc = require('./toc').toggleToc;

module.exports = {
  switcher: switcher,
  clipboard: clipboard,
  toggleToc: toggleToc
};
